# Project: 實驗室點餐系統 (AIPAR ETA)

## 語言與命名

1. 註解以繁體中文為主；變數、函式、類別命名使用英文。
2. Python 變數與函式一律使用 snake_case。
3. 路由 path slug 一律使用 kebab-case。
4. 避免使用簡體中文與中國大陸用語（如：視頻、軟件、網絡）。

## 日期及時間

1. 日期及時間一律使用 YYYY-MM-DD 格式。
2. 時間一律使用 HH:MM:SS 格式。
4. 日期及時間一律使用台北時間。

## API 與驗收規範

1. 新增或修改 API 時，至少完成以下驗收：
   - 路由可正確解析
   - 參數驗證與錯誤訊息合理
   - 主要成功流程可重現
2. 若 API 涉及背景任務或排程，需確認前端顯示狀態與實際執行狀態一致。

## 設定與安全

1. 嚴禁在程式碼中硬寫金鑰、Token、密碼、絕對機器路徑或其他機敏資訊。
2. 環境設定統一從 `.env` 讀取；缺少必要環境變數時，需回傳清楚可判讀的錯誤訊息。
3. 不得提交 `.env`、憑證檔或任何機敏設定到版本控制。

## 文件同步規範

1. 任何涉及架構、流程、資料契約、路由設計的更動，必須同步更新 `SPEC` 相關文件。
2. 若發現 `SPEC` 與實作衝突，以「程式碼實作」為準，並主動修正規格文件。

## 變更紀錄規範

1. 進行任何專案程式碼或 `SPEC` 變更，皆須更新 `CHANGELOG.md`。
2. `CHANGELOG.md` 內容需包含：
   - 變更目的（為什麼改）
   - 主要影響範圍（改了哪些模組/頁面/API）
   - 驗收方式（如何確認修正生效）
3. `CHANGELOG.md`更新時要注意最新版本號，誤將版本號重複,誤植。

## 外部版本資訊查證

1. 當涉及下列項目的版本、下載、採用建議時，必須先以當下時間查證最新且穩定資訊，再提供建議：
   - 軟體、模型、套件、專案、業界標準、標準規範、架構、法律、設計、流程、電腦零組件、文獻、網址、版本、型號。
2. 避免直接沿用過時資訊；需優先提供符合當前情境且成熟穩定的版本建議。
3. 隔壁的'AIPAR-ordering-system'專案與本專案雖名稱類似但互不隸屬，請勿未經允許直接參考其程式碼或架構。

## File Size & Structure

1. Warn at ~300 lines; hard limit 500 lines — refuse to grow past it; propose a split instead.